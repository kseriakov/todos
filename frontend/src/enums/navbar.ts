export enum UserLinksNavbar {
    TASKS = "Задачи",
}

export enum CheifLinksNavbar {
    TASKS_ON_CONTROL = "Задачи на контроле",
    MY_WORKES = "Мои сотрудники",
}

export enum GeneralLinksNavbar {
    LOGIN = "Войти",
    LOGOUT = "Выйти",
}

export enum UserRole {
    CHIEF = "CHIEF",
    WORKER = "WORKER",
}
